import React from "react";

import { RegisterContainer } from "./Register.styles";

interface Props {}

const Register = (props: Props) => {
  return <RegisterContainer>reg</RegisterContainer>;
};

export default Register;
